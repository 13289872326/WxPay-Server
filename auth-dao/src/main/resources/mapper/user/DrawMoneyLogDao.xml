<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xn.dao.user.DrawMoneyLogDao">
    <!--获取最新提现记录-->
    <select id="getLastMoneyLog">

    </select>

    <!--获取历史提现记录-->
    <select id="getUserDrawMoneyLogList">
    select * from t_draw_money_log where  userId="#{userId}"
    </select>


    <!--获取历史提现记录-->
    <update id="updateLog">

    </update>

</mapper>